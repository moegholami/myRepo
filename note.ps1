Change 1





git --version
git init
git clone src dst
git clone src

Global configurations
who am I?
git config --global --list
git config --global user.name "Mohammad Gholami"
git config --global user.email gholamy@gmail.com

git config --list --show-origin      #see where coming from :q
git config --list

git config --global init.defaultBranch main #this is very common to use instead of master

#Pay attention to the current .git folder content, especially the objects folder
git add .

new LINE

New LINE 2

New Line added in github
